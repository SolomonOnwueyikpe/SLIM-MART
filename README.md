- create virtual environment "python -m venv .venv" and activate it with [Ubuntu/Mac] "source .venv/bin/activate" [Windows] ".venv/Scripts/activate"
- install dependencies  "pip install -r requirements.txt"
- run app.py